# Development dependencies — runtime deps are not pinned here.
# Install with:  pip install --user -r requirements-dev.txt
# Or via the system package manager:
#   Fedora:  sudo dnf install python3-pytest python3-mypy
pytest>=7

# mypy is scoped to the persistence layer today (see mypy.ini); widens
# module-by-module as more files gain annotations. Run with:  mypy .
mypy>=1.10

# pyflakes finds what the suite cannot: a name used and never defined, in a
# branch no test takes. It found a logger typo inside an `except` that turned
# a handled read error into a crash. Filter its four false positives — `_`,
# `ngettext`, `book_label` and `C_` are installed on builtins by
# main._setup_gettext — and everything left is real:
#   python3 -m pyflakes *.py tests/*.py tools/*.py |
#       grep -vE "undefined name '(_|ngettext|book_label|C_)'"
pyflakes>=3
