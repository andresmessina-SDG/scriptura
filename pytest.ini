[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Add project root to sys.path so tests can `import open_data` etc. without
# packaging the project as an installable.
pythonpath = .
